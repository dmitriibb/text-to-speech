# model_catalog

Machine-readable catalog for local TTS model choices.

The Phase 0 catalog tracks:

- which models are allowed for local validation
- which model is the default validation target
- model/runtime source URLs
- what is approved
- what is Unknown and still needs legal or technical review

Primary file:

- `approved_models.json`


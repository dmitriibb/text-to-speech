## 0.1.0

- Added shared model catalog parsing and metadata types
- Added reusable model directory validation helpers
- Added pure-Dart archive extraction for portable model installs
- Added shared `sherpa_onnx` runtime wrapper and input validation helpers

# Task 10: Generation estimates UI

## Goal

Show lightweight expected generation time and expected audio length under the basic text input using the persisted per-model statistics.

## Status

- Completed.
- Basic text input now shows estimate lines when per-model stats exist and the current text is non-empty.
- Output-length estimates are speed-adjusted from stats normalized to speech speed `1.0`.

## Next Steps

- Future task: surface the same model statistics in a richer diagnostics or settings screen if needed.
